{
  "name": "repo-graph-simple",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -F web dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.19.24"
  },
  "dependencies": {
    "react-zoom-pan-pinch": "^3.7.0"
  }
}